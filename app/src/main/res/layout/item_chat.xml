<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/ivAvatar"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@drawable/default_pfp"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_circle"
        android:clipToOutline="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvHora"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10:12 pm"
        android:textColor="#95A1AC"
        android:textSize="12sp"
        app:layout_constraintTop_toTopOf="@id/ivAvatar"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvNombreTour"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Nombre (Tour)"
        android:textStyle="bold"
        android:textSize="15.5sp"
        android:textColor="@android:color/black"
        app:layout_constraintStart_toEndOf="@id/ivAvatar"
        app:layout_constraintEnd_toStartOf="@id/tvHora"
        app:layout_constraintTop_toTopOf="@id/ivAvatar"
        android:layout_marginStart="12dp" />

    <TextView
        android:id="@+id/tvPreview"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Tú: Mensaje de ejemplo"
        android:textColor="#95A1AC"
        android:textSize="13sp"
        android:singleLine="true"
        android:ellipsize="end"
        app:layout_constraintStart_toStartOf="@id/tvNombreTour"
        app:layout_constraintEnd_toStartOf="@id/badge"
        app:layout_constraintTop_toBottomOf="@id/tvNombreTour"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="32dp" />

    <FrameLayout
        android:id="@+id/badge"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:background="@drawable/badge_red"
        android:foregroundGravity="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/tvPreview"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvBadgeCount"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="1"
            android:textStyle="bold"
            android:textSize="12sp"
            android:gravity="center"
            android:textColor="@android:color/white" />
    </FrameLayout>

    <!-- BARRIER con width/height requeridos -->
    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barBottomContent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="bottom"
        app:barrierAllowsGoneWidgets="true"
        app:constraint_referenced_ids="ivAvatar,tvPreview,badge" />

    <!-- Divisor inferior anclado al Barrier -->
    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#E8ECEF"
        app:layout_constraintTop_toBottomOf="@id/barBottomContent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
